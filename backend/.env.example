# ============================================
# BASE DE DATOS - NEON (PostgreSQL)
# ============================================
# RECOMENDADO: Usar Neon para desarrollo y producción
# Obtén tus credenciales en: https://console.neon.tech
# Connection string: postgresql://[user]:[password]@[host]/[database]?sslmode=require

DB_DIALECT=postgres
DB_HOST=tu-proyecto.neon.tech
DB_PORT=5432
DB_NAME=neondb
DB_USER=tu-usuario-neon
DB_PASSWORD=tu-password-neon

# ============================================
# ALTERNATIVA: MySQL Local (con Docker)
# ============================================
# Si prefieres usar MySQL local, cambia a:
# DB_DIALECT=mysql
# DB_HOST=mysql (si usas docker-compose.yml)
# DB_PORT=3306
# DB_NAME=menu_digital
# DB_USER=menuadmin
# DB_PASSWORD=menupass123

# JWT - IMPORTANTE: Generar una clave fuerte y única para producción
# Puedes generar una con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CAMBIAR_ESTO_POR_UNA_CLAVE_SEGURA_DE_64_CARACTERES_MINIMO
JWT_EXPIRE=7d

# Puerto del servidor
PORT=5000

# URL del Frontend (para CORS y enlaces de invitación)
FRONTEND_URL=http://localhost:3000

# Configuración de Email (Gmail)
# Necesitas crear una "App Password" en tu cuenta de Gmail:
# 1. Ve a: https://myaccount.google.com/security
# 2. Activa "Verificación en 2 pasos"
# 3. Ve a "Contraseñas de aplicaciones"
# 4. Genera una contraseña para "Correo"
EMAIL_USER=tu-email@gmail.com
EMAIL_PASSWORD=tu_app_password_de_gmail

# Email del Superadministrador (recibirá notificaciones de nuevas solicitudes)
SUPERADMIN_EMAIL=superadmin@tudominio.com

# Ambiente
NODE_ENV=development
